from setuptools import setup

setup(name='mono_client',
      version='0.1',
      description='Mono client',
      author='Wojciech Jozwiak',
      license='MIT',
      packages=['mono_client'],
      zip_safe=False)
