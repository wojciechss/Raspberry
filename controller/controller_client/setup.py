from setuptools import setup

setup(name='controller_client',
      version='0.1',
      description='Controller client',
      author='Wojciech Jozwiak',
      license='MIT',
      packages=['controller_client'],
      zip_safe=False)
